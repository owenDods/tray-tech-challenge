import PropTypes from 'prop-types';

export default {
	name: PropTypes.string,
	role: PropTypes.string,
	email: PropTypes.string,
	password: PropTypes.string,
	receiveUpdates: PropTypes.bool,
	receiveUpdatesOther: PropTypes.bool
};
